name: Build APK
on: [push, workflow_dispatch]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-python@v5
      with:
        python-version: '3.9'
    - run: sudo apt-get update
    - run: sudo apt-get install -y git zip unzip openjdk-17-jdk python3-pip autoconf libtool pkg-config zlib1g-dev libncurses5-dev libncursesw5-dev libtinfo5 cmake libffi-dev libssl-dev
    - run: pip install buildozer
    - run: pip install "jnius>=1.4.0"
    - run: buildozer -v android debug
    - uses: actions/upload-artifact@v4
      with:
        name: kivy-apk
        path: bin/*.apk
